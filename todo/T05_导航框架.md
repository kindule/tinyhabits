# T05: 页面导航框架

> **优先级**: P0 (必须实现)
> **预估工时**: 0.5天
> **负责人**: 前端开发
> **状态**: ✅ DONE

---

## 一、任务描述

实现页面导航框架，包括页面路由配置、导航守卫(自动跳转未完成的流程页)、页面切换动画配置。

---

## 二、验收标准

- [ ] 5个主要页面路由配置完成
- [ ] 首次进入小程序自动跳转Onboarding页
- [ ] 根据流程状态自动跳转到正确的页面
- [ ] 页面切换平滑无闪烁
- [ ] 支持返回上一页功能

---

## 三、技术实现

### 3.1 页面路由配置 `app.json`

```json
{
  "pages": [
    "pages/onboarding/index",
    "pages/role/index",
    "pages/wishes/index",
    "pages/workshop/index",
    "pages/daily/index"
  ],
  "window": {
    "backgroundTextStyle": "light",
    "navigationBarBackgroundColor": "#FAFBFF",
    "navigationBarTitleText": "点点",
    "navigationBarTextStyle": "black",
    "backgroundColor": "#FAFBFF",
    "navigationStyle": "custom"
  },
  "style": "v2"
}
```

### 3.2 导航工具函数 `utils/navigation.js`

```javascript
const { loadState } = require('./state.js');

/**
 * 页面路径常量
 */
const PAGES = {
  ONBOARDING: '/pages/onboarding/index',
  ROLE: '/pages/role/index',
  WISHES: '/pages/wishes/index',
  WORKSHOP: '/pages/workshop/index',
  DAILY: '/pages/daily/index'
};

/**
 * 获取首页路径(根据状态决定)
 * @returns {string} 页面路径
 */
function getHomePage() {
  const state = loadState();
  
  // 新用户进入Onboarding
  if (!state.hasRole && !state.hasWishRank && !state.hasDeck) {
    return PAGES.ONBOARDING;
  }
  
  // 已有状态的用户，根据流程跳转
  if (!state.hasRole) {
    return PAGES.ROLE;
  }
  if (!state.hasWishRank) {
    return PAGES.WISHES;
  }
  if (!state.hasDeck) {
    return PAGES.WORKSHOP;
  }
  
  return PAGES.DAILY;
}

/**
 * 导航到指定页面(replace当前页)
 * @param {string} url 页面路径
 */
function redirectTo(url) {
  wx.redirectTo({ url });
}

/**
 * 导航到指定页面(保留当前页)
 * @param {string} url 页面路径
 */
function navigateTo(url) {
  wx.navigateTo({ url });
}

/**
 * 导航到首页(根据状态)
 */
function navigateToHome() {
  const homePage = getHomePage();
  redirectTo(homePage);
}

/**
 * 导航到下一个流程页面
 * @param {string} currentPage 当前页面
 */
function navigateToNext(currentPage) {
  const flowOrder = [
    PAGES.ONBOARDING,
    PAGES.ROLE,
    PAGES.WISHES,
    PAGES.WORKSHOP,
    PAGES.DAILY
  ];
  
  const currentIndex = flowOrder.indexOf(currentPage);
  if (currentIndex >= 0 && currentIndex < flowOrder.length - 1) {
    redirectTo(flowOrder[currentIndex + 1]);
  }
}

/**
 * 页面加载时的流程验证(在页面onLoad中调用)
 * @param {string} currentPage 当前页面路径
 * @returns {boolean} 是否需要重定向
 */
function validatePageAccess(currentPage) {
  const state = loadState();
  
  // Daily页需要验证前置流程
  if (currentPage === PAGES.DAILY) {
    if (!state.hasRole) {
      redirectTo(PAGES.ROLE);
      return true;
    }
    if (!state.hasWishRank) {
      redirectTo(PAGES.WISHES);
      return true;
    }
    if (!state.hasDeck) {
      redirectTo(PAGES.WORKSHOP);
      return true;
    }
  }
  
  // Workshop页需要验证愿望排序
  if (currentPage === PAGES.WORKSHOP) {
    if (!state.hasRole) {
      redirectTo(PAGES.ROLE);
      return true;
    }
    if (!state.hasWishRank) {
      redirectTo(PAGES.WISHES);
      return true;
    }
  }
  
  // Wishes页需要验证角色创建
  if (currentPage === PAGES.WISHES) {
    if (!state.hasRole) {
      redirectTo(PAGES.ROLE);
      return true;
    }
  }
  
  return false;
}

module.exports = {
  PAGES,
  getHomePage,
  redirectTo,
  navigateTo,
  navigateToHome,
  navigateToNext,
  validatePageAccess
};
```

### 3.3 App入口处理 `app.js`

```javascript
const { getHomePage, redirectTo } = require('./utils/navigation.js');
const { refreshDailyState } = require('./utils/flow.js');

App({
  onLaunch() {
    // 检查更新
    this.checkUpdate();
  },
  
  onShow(options) {
    // 刷新每日状态
    refreshDailyState();
    
    // 如果是从分享进入等场景，需要验证页面
    if (options.scene === 1007 || options.scene === 1008) {
      // 从分享进入，跳转到正确的首页
      const homePage = getHomePage();
      const pages = getCurrentPages();
      if (pages.length === 0) {
        redirectTo(homePage);
      }
    }
  },
  
  globalData: {
    version: '1.0.0-MVP'
  },
  
  checkUpdate() {
    if (wx.canIUse('getUpdateManager')) {
      const updateManager = wx.getUpdateManager();
      updateManager.onCheckForUpdate((res) => {
        if (res.hasUpdate) {
          console.log('发现新版本');
        }
      });
      updateManager.onUpdateReady(() => {
        wx.showModal({
          title: '更新提示',
          content: '新版本已准备好，是否重启应用？',
          success: (res) => {
            if (res.confirm) {
              updateManager.applyUpdate();
            }
          }
        });
      });
    }
  }
});
```

### 3.4 页面基类Mixin

```javascript
// utils/page-mixin.js

const { validatePageAccess, PAGES } = require('./navigation.js');

/**
 * 创建带有流程验证的页面配置
 * @param {string} pagePath 当前页面路径
 * @param {Object} pageConfig 页面配置对象
 * @returns {Object} 增强后的页面配置
 */
function createPage(pagePath, pageConfig) {
  const originalOnLoad = pageConfig.onLoad;
  
  pageConfig.onLoad = function(options) {
    // 先验证页面访问权限
    const needRedirect = validatePageAccess(pagePath);
    if (needRedirect) {
      return; // 已重定向，不执行后续逻辑
    }
    
    // 调用原始onLoad
    if (originalOnLoad) {
      originalOnLoad.call(this, options);
    }
  };
  
  return pageConfig;
}

module.exports = {
  createPage,
  PAGES
};
```

---

## 四、页面使用示例

```javascript
// pages/daily/index.js
const { createPage, PAGES } = require('../../utils/page-mixin.js');

Page(createPage(PAGES.DAILY, {
  data: {
    // 页面数据
  },
  
  onLoad(options) {
    // 页面初始化逻辑
    // 此时已通过流程验证
    this.loadDailyCard();
  },
  
  loadDailyCard() {
    // 加载每日卡片
  }
}));
```

---

## 五、测试要点

1. **新用户场景**: 首次打开跳转到Onboarding
2. **中断恢复**: 在Wishes页退出，重新打开应回到Wishes
3. **完整用户**: 完成所有流程后，打开直接进入Daily
4. **防跳页验证**: 直接访问Daily页应被拦截

---

## 六、交付物

- [x] `utils/navigation.js` - 导航工具
- [x] `utils/page-mixin.js` - 页面Mixin
- [x] `app.js` 入口配置
- [x] `app.json` 路由配置

---

## 七、备注

- 使用`redirectTo`而非`navigateTo`避免页面栈过深
- 自定义导航栏(`navigationStyle: custom`)需要各页面自行实现
- 考虑后续添加页面切换动画
