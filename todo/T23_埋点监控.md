# T23: 埋点与数据监控

> **优先级**: P1 (应该实现)
> **预估工时**: 1天
> **负责人**: 前端/数据开发
> **状态**: ✅ DONE

---

## 一、任务描述

实现数据埋点和监控系统，跟踪用户行为、验证产品假设、监控技术指标。

---

## 二、验收标准

- [ ] 核心事件埋点完成
- [ ] 用户属性上报
- [ ] 错误监控
- [ ] 性能监控
- [ ] 数据仪表盘可查看

---

## 三、事件埋点设计

### 3.1 页面访问事件

| 事件名 | 参数 | 说明 |
|--------|------|------|
| `page_view` | page_path, page_title | 页面访问 |
| `page_leave` | page_path, stay_duration | 页面离开 |

### 3.2 核心流程事件

| 事件名 | 参数 | 说明 |
|--------|------|------|
| `onboarding_start` | - | 开始Onboarding |
| `onboarding_complete` | duration | 完成Onboarding |
| `role_select` | talent_type | 选择天赋 |
| `role_confirm` | talent_type | 确认天赋 |
| `wish_battle` | round, winner_id, loser_id | 愿望对决 |
| `wish_rank_complete` | core_wish, duration | 完成愿望排序 |
| `wish_swap` | old_core, new_core | 交换核心愿望 |
| `workshop_summon` | count | 召唤灵感 |
| `workshop_anchor_select` | behavior_id, anchor_text | 选择锚点 |
| `workshop_split` | behavior_id | 行为裂变 |
| `workshop_merge` | behavior_a, behavior_b | 行为合成 |
| `deck_generate` | card_count | 生成卡组 |
| `deck_confirm` | deck_summary | 确认卡组 |
| `daily_start` | day, card_id | 开始施放 |
| `daily_complete` | day, card_id, streak | 完成任务 |
| `rift_shatter` | message | 击碎裂隙 |
| `day7_complete` | best_streak, total_score | 完成7天周期 |
| `day7_choice` | choice_type | 选择下一步(new_wish/continue) |

### 3.3 功能使用事件

| 事件名 | 参数 | 说明 |
|--------|------|------|
| `ai_anchor_request` | behavior_name | 请求AI锚点 |
| `ai_anchor_success` | anchor_count, latency | AI锚点成功 |
| `ai_anchor_fallback` | reason | AI锚点降级 |
| `ai_map_request` | deck_size | 请求MAP评估 |
| `ai_map_success` | overall, latency | MAP评估成功 |
| `ai_map_fallback` | reason | MAP评估降级 |
| `rescue_card_shown` | days_missed | 显示救援卡 |
| `rescue_complete` | - | 救援完成 |

### 3.4 错误事件

| 事件名 | 参数 | 说明 |
|--------|------|------|
| `error_js` | message, stack, page | JS错误 |
| `error_api` | url, status, message | API错误 |
| `error_storage` | operation, message | 存储错误 |

---

## 四、技术实现

### 4.1 埋点工具封装 `utils/analytics.js`

```javascript
/**
 * 数据埋点工具
 */

// 配置
const config = {
  enabled: true,
  debug: process.env.NODE_ENV === 'development',
  batchSize: 10,
  flushInterval: 30000 // 30秒批量上报
};

// 事件队列
let eventQueue = [];
let flushTimer = null;

/**
 * 初始化埋点
 */
function init() {
  // 启动定时上报
  flushTimer = setInterval(flush, config.flushInterval);
  
  // 监听全局错误
  wx.onError((error) => {
    trackError('js', error);
  });
  
  // 监听网络错误
  wx.onNetworkStatusChange((res) => {
    track('network_change', {
      is_connected: res.isConnected,
      network_type: res.networkType
    });
  });
}

/**
 * 跟踪事件
 * @param {string} eventName 事件名
 * @param {Object} properties 事件属性
 */
function track(eventName, properties = {}) {
  if (!config.enabled) return;
  
  const event = {
    event: eventName,
    properties: {
      ...properties,
      timestamp: Date.now(),
      session_id: getSessionId(),
      page: getCurrentPagePath()
    }
  };
  
  if (config.debug) {
    console.log('[Analytics]', eventName, properties);
  }
  
  eventQueue.push(event);
  
  // 达到批量大小则立即上报
  if (eventQueue.length >= config.batchSize) {
    flush();
  }
}

/**
 * 跟踪错误
 */
function trackError(type, error) {
  track(`error_${type}`, {
    message: error.message || String(error),
    stack: error.stack,
    type
  });
}

/**
 * 设置用户属性
 * @param {Object} properties 用户属性
 */
function setUserProperties(properties) {
  // 存储到本地
  try {
    const existing = wx.getStorageSync('user_properties') || {};
    wx.setStorageSync('user_properties', { ...existing, ...properties });
  } catch (e) {
    console.error('设置用户属性失败:', e);
  }
  
  // 上报
  track('user_profile', properties);
}

/**
 * 批量上报事件
 */
async function flush() {
  if (eventQueue.length === 0) return;
  
  const events = [...eventQueue];
  eventQueue = [];
  
  try {
    // 获取用户属性
    const userProps = wx.getStorageSync('user_properties') || {};
    
    // 上报到服务器
    await wx.request({
      url: 'https://api.tinyhabits.com/api/analytics/events',
      method: 'POST',
      data: {
        events,
        user: userProps,
        device: getDeviceInfo()
      },
      timeout: 5000
    });
    
  } catch (error) {
    // 上报失败，放回队列
    eventQueue = [...events, ...eventQueue];
    console.error('埋点上报失败:', error);
  }
}

/**
 * 获取会话ID
 */
function getSessionId() {
  let sessionId = wx.getStorageSync('session_id');
  if (!sessionId) {
    sessionId = `s_${Date.now()}_${Math.random().toString(36).slice(2)}`;
    wx.setStorageSync('session_id', sessionId);
  }
  return sessionId;
}

/**
 * 获取当前页面路径
 */
function getCurrentPagePath() {
  const pages = getCurrentPages();
  if (pages.length > 0) {
    return pages[pages.length - 1].route;
  }
  return '';
}

/**
 * 获取设备信息
 */
function getDeviceInfo() {
  try {
    const systemInfo = wx.getSystemInfoSync();
    return {
      platform: systemInfo.platform,
      system: systemInfo.system,
      brand: systemInfo.brand,
      model: systemInfo.model,
      screenWidth: systemInfo.screenWidth,
      screenHeight: systemInfo.screenHeight,
      wechatVersion: systemInfo.version,
      sdkVersion: systemInfo.SDKVersion
    };
  } catch (e) {
    return {};
  }
}

/**
 * 页面访问跟踪
 */
function trackPageView(pagePath, pageTitle) {
  track('page_view', {
    page_path: pagePath,
    page_title: pageTitle
  });
}

/**
 * 页面离开跟踪
 */
function trackPageLeave(pagePath, stayDuration) {
  track('page_leave', {
    page_path: pagePath,
    stay_duration: stayDuration
  });
}

/**
 * 页面Mixin - 自动跟踪页面访问
 */
function createTrackedPage(pagePath, pageConfig) {
  let enterTime = 0;
  
  const originalOnLoad = pageConfig.onLoad;
  const originalOnUnload = pageConfig.onUnload;
  const originalOnHide = pageConfig.onHide;
  
  pageConfig.onLoad = function(options) {
    enterTime = Date.now();
    trackPageView(pagePath, pageConfig.pageTitle || pagePath);
    if (originalOnLoad) {
      originalOnLoad.call(this, options);
    }
  };
  
  pageConfig.onUnload = function() {
    const stayDuration = Date.now() - enterTime;
    trackPageLeave(pagePath, stayDuration);
    if (originalOnUnload) {
      originalOnUnload.call(this);
    }
  };
  
  pageConfig.onHide = function() {
    const stayDuration = Date.now() - enterTime;
    trackPageLeave(pagePath, stayDuration);
    if (originalOnHide) {
      originalOnHide.call(this);
    }
  };
  
  return pageConfig;
}

module.exports = {
  init,
  track,
  trackError,
  setUserProperties,
  flush,
  trackPageView,
  trackPageLeave,
  createTrackedPage
};
```

### 4.2 用户属性定义

```javascript
/**
 * 用户属性更新
 * 在关键节点调用setUserProperties
 */

// 完成Onboarding后
setUserProperties({
  has_completed_onboarding: true,
  onboarding_date: new Date().toISOString()
});

// 选择天赋后
setUserProperties({
  talent_type: 'fire',
  has_role: true
});

// 完成愿望排序后
setUserProperties({
  core_wish: '健康',
  has_wish_rank: true
});

// 生成卡组后
setUserProperties({
  has_deck: true
});

// 完成每日任务后
setUserProperties({
  current_day: 2,
  current_streak: 2,
  total_completions: 2,
  last_complete_date: new Date().toISOString()
});

// 完成7天周期后
setUserProperties({
  cycles_completed: 1,
  best_streak: 7,
  total_score: 70
});
```

### 4.3 后端埋点接收

```javascript
// server/src/routes/analytics.js

const express = require('express');
const router = express.Router();

/**
 * 接收埋点数据
 * POST /api/analytics/events
 */
router.post('/events', async (req, res) => {
  try {
    const { events, user, device } = req.body;
    
    // 验证数据
    if (!Array.isArray(events)) {
      return res.status(400).json({ success: false, error: 'Invalid events' });
    }
    
    // 处理事件(这里可以存储到数据库或转发到数据平台)
    const enrichedEvents = events.map(event => ({
      ...event,
      user,
      device,
      server_time: Date.now()
    }));
    
    // TODO: 存储到MongoDB或发送到数据仓库
    console.log(`Received ${enrichedEvents.length} events`);
    
    // 异步处理，快速响应
    res.json({ success: true, received: events.length });
    
  } catch (error) {
    console.error('Analytics error:', error);
    res.status(500).json({ success: false, error: error.message });
  }
});

module.exports = router;
```

---

## 五、关键指标仪表盘

### 5.1 核心指标

| 指标 | 计算方式 | 目标值 |
|------|----------|--------|
| **DAU** | 日活跃用户数 | - |
| **新用户完成率** | 完成首日任务/新用户 | >60% |
| **7日留存率** | 第7天活跃/第1天新增 | >30% |
| **日均完成率** | 完成任务用户/当日活跃 | >50% |
| **平均Streak** | 所有用户Streak均值 | >3天 |
| **周期完成率** | 完成7天周期/开始周期 | >20% |

### 5.2 功能使用指标

| 指标 | 说明 |
|------|------|
| 愿望排序完成率 | 完成7轮对决的用户比例 |
| 召唤灵感使用率 | 使用召唤功能的用户比例 |
| 裂变使用率 | 使用裂变功能的用户比例 |
| 合成使用率 | 使用合成功能的用户比例 |
| AI锚点使用率 | 使用AI锚点的用户比例 |
| 救援卡出现率 | 出现救援卡的用户比例 |

### 5.3 技术指标

| 指标 | 说明 | 目标值 |
|------|------|--------|
| 页面加载时间 | 首屏加载P90 | <1s |
| API响应时间 | 接口响应P90 | <500ms |
| AI接口成功率 | AI接口成功比例 | >95% |
| JS错误率 | 错误PV/总PV | <0.1% |
| 崩溃率 | 崩溃会话/总会话 | <1% |

---

## 六、监控告警

### 6.1 告警规则

| 规则 | 条件 | 级别 |
|------|------|------|
| API高错误率 | 5分钟内错误率>5% | P1 |
| AI服务不可用 | 连续3次调用失败 | P1 |
| JS错误飙升 | 1小时内错误>100 | P2 |
| 新用户转化低 | 日新用户完成率<40% | P3 |
| DAU异常下降 | 日环比下降>30% | P2 |

---

## 七、交付物

- [x] `utils/analytics.js` - 埋点工具
- [x] 事件埋点文档
- [x] 用户属性文档
- [x] 后端接收接口
- [x] 监控告警规则
